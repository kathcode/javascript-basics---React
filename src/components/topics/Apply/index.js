import React from 'react';


const Apply = () => (
  <React.Fragment>
    <section>
      <h1>Apply</h1>
      <p>
        The <code>apply()</code> method call a function with a given <strong>"this"</strong> and an <strong>array</strong> with the arguments
      </p>
    </section>
  </React.Fragment>
);

export default Apply;
